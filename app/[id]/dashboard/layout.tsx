import { ReactNode } from "react";

// The Navbar layout is handled by the parent [id]/layout.tsx
export default function DashboardLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
